### 一些需要注意的地方

1. **损失函数**

   **其次，我们将引入一个新的基准（Benchmark）来评估模型** 。然而，本次作业的重点是弥合基础模型与下游任务之间的差距（后训练），因此我们必须使用独立于交叉熵的评估方法 。我们将使用来自 Hendrycks 等人（2021）的 **MATH 12K 数据集**，该数据集由极具挑战性的高中数学竞赛题目组成 。我们将通过将模型输出与参考答案进行对比来评估模型 。

   **评估范式的转变**：不再仅仅通过预测下一个 token 的准确性（交叉熵损失）来衡量模型好坏，而是通过**最终答案的正确性**来衡量 。这反映了业界从关注“模型学得像不像”到关注“模型能不能解决问题”的转变。

2. **思维链**

​       思维链是指在得出最终答案之前，逐步思考问题并生成中间推理步骤的过程 。

**大语言模型的思维链推理**：早期的思维链方法通过使用“草稿本”（scratchpad）将问题拆解为中间步骤，从而微调模型解决简单的算术任务 。其他研究通过提示（Prompt）让强大的模型在回答前“一步步思考”，发现这显著提高了在年级数学题等推理任务上的表现 。

**通过专家迭代（Expert Iteration）学习推理**：自我提升推理者（STaR）框架将推理视为一个引导循环：预训练模型首先采样多种多样的思维链，仅保留那些导致正确答案的样本，然后对这些“专家”轨迹进行微调 。迭代这一循环可以提高模型的推理能力和解题率 。STaR 证明了这种使用基于字符串匹配的自动验证方法，可以在没有人类编写推理轨迹的情况下引导出推理技能 。

**通过验证奖励、o1 和 R1 进行推理强化学习**：最近的研究探索了使用更强大的强化学习算法和验证奖励来提高推理性能 。OpenAI 的 **o1**、DeepSeek 的 **R1** 以及 Moonshot 的 **Kimi k1.5** 使用策略梯度方法，在数学和代码任务上进行训练，通过字符串匹配或单元测试验证正确性，在竞赛数学和编程表现上取得了显著进步 。随后的工作证实，即使是在 1.5B 参数的小模型上，纯粹的验证奖励强化学习也能提升推理表现 。

### Temp与Top-p选择

在低难度任务中，我们追求**稳定性（Reliability）**；而在竞赛级任务中，我们更依赖模型通过高气温（Temperature）触发不同的**思维链（Chain of Thought, CoT）**路径，从而增加解出难题的概率（例如通过 Self-Consistency 策略）。

T遵循如下公式：
$$
p_i = \frac{\exp(z_i / T)}{\sum_j \exp(z_j / T)}
$$
top_p计算：

**排序**：将模型输出的词表中所有候选 token，按照概率从高到低进行降序排列。

**累加与截断**：从概率最高的 token 开始，依次累加它们的概率值。当累加和刚好达到或首次超过 **0.95** 时，画一条线，丢弃（截断）排在这条线之后的所有低概率 token。

**重归一化与采样**：保留下来的这部分 token 构成了候选池（即“核”）。框架会将这些 token 的概率重新归一化（使其总和等于 1），最后在这个缩小且安全的范围内进行随机采样。

| **数据集分类**  | **包含数据集**           | **难度等级**        | **Temperature (T)** | **Top-p** | **采样策略建议**                                             |
| --------------- | ------------------------ | ------------------- | ------------------- | --------- | ------------------------------------------------------------ |
| **基础数学**    | **GSM8K**                | 小学/初中应用题     | $0$                 | $0.95$    | **Greedy Decoding**: 确保逻辑路径唯一，减少低级计算失误。    |
| **中高难度**    | **AMC, MATH (MATH-500)** | 高中/大学预科竞赛   | $0.7$               | $0.95$    | **Diversity Sampling**: 允许模型尝试不同的代数变形或辅助线思路。 |
| **奥数/竞赛级** | **AIME, OmniMATH**       | 国家级奥赛/前沿数学 | $0.7$               | $0.95$    | **Exploratory Reasoning**: 配合多次采样（Sample $N$）来覆盖极难问题的解空间。 |
| **年度专项**    | **AIME-2024, AIME-2025** | 最新竞赛真题        | $0.7$               | $0.95$    | **Zero-shot CoT**: 避免过拟合旧题库，利用高 $T$ 激发原生推理能力。 |

### baseline

#### 模型：

Qwen 2.5 Math 1.5B推理

#### prompt:

为了能够完成答案与思考的检验，本文使用r1_zero 提示prompt，这个prompt强制让推理过程打上 `<think>` 标签，**并将最终的结论包裹在 `<answer>` 标签中。** **这种设计的核心作用主要有两点**

- **实现自动化提取与精确评分**：通过规范化的结构，后续的奖励函数（Reward Function）能够利用正则表达式，轻松剥离长篇大论的推理步骤，精准提取出最终答案，并与标准答案（Ground Truth）进行数学等价性验证。
- **提供格式约束与奖励信号**：在强化学习（如 GRPO）的训练过程中，标签的完整性本身就被作为一种基础的“格式奖励（Format Reward）”。这能有效引导模型在训练初期快速收敛，固化出“先深度思考、后严格作答”的规范思维链（CoT）范式。

#### 评判标准

**1. 格式奖励 (Format Reward) 评判标准**

- **得 1 分**：模型输出必须严格包含完整的闭合标签。且 `</think>` 与 `<answer>` 之间**允许有1个空格/制表符，但最多只能包含一个换行符**。
- **得 0 分**：缺少 `<answer>` 或 `</answer>` 标签，或者在思考与作答阶段之间使用了过多的换行符（如 `\n\n`）破坏了规定的排版格式。

**2.答案奖励 (Answer Reward) 评判标准**

- **得 1 分**：在**格式得分为 1 的前提下**，成功提取 `<answer>` 标签内（或其中的 `\boxed{}` 内）的文本，并经过规范化处理（如去除单位、化简分数、统一浮点数精度）后，与标准答案（Ground Truth）在数学逻辑上**完全等价**（通过 SymPy 解析或 MathD 匹配）。
- 有Box提取Box，没box全部提取出来
- extract_answer函数就是分离Box的，没有返回空
- **得 0 分**：答案在数学上错误、提取失败、或者格式本身就错误。

最终的**总奖励（Reward）**采取严格的一票否决制。即使格式正确（格式奖励 = 1.0），只要答案错误，**总奖励依然归零（Reward = 0.0）**。这能有效防止模型在 RL 训练中通过“只输出漂亮格式但不认真做题”来骗取分数。

#### 结果

|   **数据集**   | **测试样本数** | **正确率** | **格式错误率** | **长度 (平均token)** | **成功样本平均长度token** | **错误回答平均长度token** |
| :------------: | :------------: | :--------: | :------------: | :------------------: | :-----------------------: | :-----------------------: |
|   **GSM8K**    |      1319      |   21.61%   |     35.78%     |        226.63        |          142.86           |          249.72           |
|    **AMC**     |       83       |   4.82%    |     75.90%     |        733.35        |          170.50           |          761.85           |
|  **MATH-500**  |      500       |   9.20%    |     64.60%     |        423.43        |          169.04           |          449.20           |
| **AIME_2024**  |       30       |   0.00%    |     86.67%     |        791.77        |           0.00            |          791.77           |
| **AIME_2025**  |       30       |   0.00%    |     80.00%     |       1114.37        |           0.00            |          1114.37          |
|  **OmniMATH**  |      4428      |   2.57%    |     77.57%     |        811.61        |          171.39           |          828.53           |
| **Bespoke17k** |      276       |   10.87%   |     71.74%     |        329.07        |          160.47           |          349.63           |

### SFT监督微调

#### 监督微调伪代码

**算法 1：监督微调 (Supervised Finetuning, SFT)** 

- **输入**: 初始策略模型 $\pi_{\theta_{init}}$ ；SFT 数据集 $\mathcal{D}$ 
- 1: 策略模型初始化：$\pi_{\theta} \leftarrow \pi_{\theta_{init}}$ 
- 2: **for** 步骤 $= 1, ..., n\_sft\_steps$ **do** 
- 3: $\quad$ 从数据集 $\mathcal{D}$ 中采样一批（batch）问答对 $\mathcal{D}_{b}$ 
- 4: $\quad$ 使用模型 $\pi_{\theta}$ 计算给定问题的回答的交叉熵损失 
- 5: $\quad$ 根据交叉熵损失进行梯度步进（gradient step），以更新模型参数 $\theta$ 
- 6: **end for** 
- **输出**: 训练后的策略模型 $\pi_{\theta}$ 

#### 数据集的制作标准

所有脚本均采用 $R1-Zero$ 提示词标准，强制要求模型输出以 `<think>` 标签开始，并在提示词末尾预留该标签以引导模型进行思维链（CoT）推理。

##### train数据集

**RL (强化学习) 格式**：包含 `prompt`、`question` 和 `answer` 字段，主要用于基线评估及 $GRPO$ 等强化学习训练。

**SFT (监督微调) 格式**：包含 `prompt`、`response` 和 `ground_truth`。其中 `response` 被构造为符合推理规范的结构：`[推理过程]\n</think>\n<answer> [最终答案] </answer>`

##### test数据集

与train格式完全一样。

#### GSM8K数据集

将GSM8K进行转化，文档中说明的格式是一个包含格式化提示和目标回答的 JSON 元素：`{"prompt": str, "response": str}` 。其中目标回答包含了思维链推理轨迹和最终答案 。

GSM8K数据集是是**人类专家**写的标准解析， 前面是一段非常精简、毫无废话的解题步骤，然后用 `####` 隔开，最后是数字答案。因此需要用think标签和answer标签进行包裹（强依赖，评判基准）

最后的格式类似如此：{"prompt": str, "response": str}，其中promot可以用上一个baseline处理方式，question替换方式，response进行标签的插入。

SFT的特点就是**交叉熵**损失，必须一字不拉的学习。

掩码：prompt必须全部掩码，还有padding这些。prompt的一些提示对于训练没有帮助。

#### Bespoke-Stratos-17k 数据集

Bespoke-Stratos-17k 数据集存在AI生成的，因此长度和数据集质量十分不稳定，因此要对他进行如下清洗：

1. **标签替换**：`HuggingFaceH4/Bespoke-Stratos-17k` 中，它使用的是另一套特殊的标记词：
   - **思考过程**：`<|begin_of_thought|>` 和 `<|end_of_thought|>`
   - **最终答案**：`<|begin_of_solution|>` 和 `<|end_of_solution|>`
2. **结构检查**：所有的标签，包括["<think>", "</think>", "<answer>", "</answer>"只能有一个，切必须按照上述顺序实现：`开始思考 -> 结束思考 -> 开始回答 -> 结束回答`。
3. **硬性要求**：
   - 思考标签里面不允许有答案，只要提取出来\box里面的内容就删掉
   - answer里面必须有\boxed{...}，没有的也会直接删掉
   - answer里面长度太长（大于100），不直接丢弃，而是用正则去除核心答案，并重新拼装为response，而且answer里面有换行的话，会消除
   - 精确提取答案部分，然后使用extract_answer获取仅有\box部分的答案作为groud_truth
4. **长度限制**：过滤掉总 Token 数不在 $[500, 5120]$ 范围内的样本。这保证了思维链既不能太短（缺乏推理过程），也不能太长（超出模型上下文或过于冗余）。
5. box里面存在长度很多的，这个不好学，为了降低学习负担，我们把训练长度限制到了15.

##### --- 过滤总结报告 ---
原始数据总量: 16610 条
清洗后剩余量: 2763 条 (存活率: 16.63%)  正确样本2420 验证集 273个

具体处理状态分布：
  boxed_in_think: 6679
  missing_ground_truth: 5378
  fixed_and_passed: 3243
  boxed_content_too_long: 1310
  length_out_of_range: 480
  missing_qa: 0
  extract_answer_failed_on_gt: 0

过滤后数据集大小 (Correct Only): 2534

### AIME、MATH (math-500)、AMC、OmniMATH

| **数据集**                                      | **背景与来源**                                               | **题目形式**                                                 | **难度与定位**                                               | **在大模型评估中的核心作用**                                 |
| ----------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **GSM8K** (Grade School Math 8K)                | 由 OpenAI 于 2021 年发布，包含 8500 道高质量的小学级别数学应用题，是自然语言处理领域最经典的数学基准。 | 纯文本应用题（Word Problems），解题通常只需 2 到 8 步的基础四则运算（加减乘除）。 | **基础（小学级）** 不涉及代数方程或高等数学，纯考察基础算术与自然语言逻辑的转换。 | **大模型数学能力的“及格线”与基石。** 目前前沿大模型在该数据集上已接近满分（>95%），它现在主要作为小参数模型（如 1.5B/7B）或新架构的基础逻辑测试（Sanity Check），确保模型没有失去基本的算术常识。 |
| **AMC** (American Mathematics Competitions)     | 源自美国传统的初高中数学竞赛（分为 AMC 8、10、12）。它是全美数学奥林匹克体系的第一关。 | 单项选择题（通常每套卷子 25 题）。                           | **中等（初高中竞赛）** 覆盖代数、几何、组合的基础计算与逻辑推演。 | **进阶常识测试。** 通常作为评估模型是否具备“中学数学常识”的门槛。如果在 AMC 上表现不佳，通常无法胜任更高级的推理任务。 |
| **MATH** **(math-500)**                         | 源自 Hendrycks 等人 2021 年提出的数据集（1.25 万道题）。**math-500** 是 OpenAI 专门从中挑选的 500 道高质量子集，用于过程监督。 | 解答题，要求输出推导过程，最终答案使用 LaTeX 格式提取（如 $\boxed{答案}$）。 | **中到高（细分领域）** 分 1 到 5 级难度，涵盖代数、几何、数论、概率等 7 个核心数学分支。 | **主流模型的“试金石”。** math-500 是目前测试模型多步推理（CoT）和训练奖励模型（PRM/ORM）最常用、最高效的标准测试集。 |
| (American Invitational Mathematics Examination) | 美国数学邀请赛，是 AMC 的晋级赛，也是通往美国数学奥林匹克（USAMO）的关键选拔赛。 | 填空题（共 15 题），答案必须是 $000$ 到 $999$ 之间的**整数**。无选择题猜测空间。 | **极高（顶尖高中生）** 需要极强的数学直觉、深度计算规划和多步逻辑推导。 | **强化学习与深度推理的终极测试。** OpenAI o1 和 DeepSeek-R1 等主打强化学习和长思考（RL/Long-CoT）的模型，均将 AIME 作为衡量其最高推理能力的核心战力指标。 |
| **OmniMATH**                                    | 2024 年由北京大学和阿里巴巴等机构联合推出，专门为了应对前沿大模型在传统数据集上“刷榜饱和”而设计的全新基准。 | 包含 4428 道严格人工标注的竞赛级解答题，细分为 33 个数学子领域。 | **极高（奥林匹克级）** 题目多源于 IMO、IMC 等全球顶级赛事，难度通常在 AIME 之上。 | **探索前沿模型的极限与防作弊。** 专为测试当前最强模型（如 o1 等）的极限短板，同时由于是新构建的数据集，能有效防止传统考题被混入训练集导致的“数据污染”问题。 |

Bespoke-Stratos-17的测试集数量太小，分离训练集自制的测试集难度太大。正确率很低，因此我们使用Bespoke-Stratos-17k 训练后，用这些数据集测试



#### **梯度累计技术**

尽管模型采用bfloat16浮点型数据结构并使用FlashAttention-2注意力机制,但即便是80GB显  存的GPU也难以支持合理的批量处理规模。为实现更大批量计算,我们可以采用名为梯度累积的技术。该  技术的核心思想是:无需在每个批次后更新模型权重(即执行优化器步骤),而是将多个批次的梯度值累  积后再进行梯度更新。直观来说,若使用更大容量的GPU,我们应当能像一次性处理32个样本批次那样获  得相同计算结果。将样本分为16个批次,每批次2个样本,最后取平均值。

梯度累积在PyTorch中实现起来非常简单。需要说明的是,每个权重张量都包含一个存储梯度的属性.  grad。在调用loss.backward()函数前,该属性值为None;执行loss.backward()后,属性值即为梯度。通常我们  会先执行优化器的一步操作,再通过optimizer.zero_grad()()将梯度重置为零,从而清空权重张量的.grad字  段。

#### 损失与熵计算

在训练阶段，我们使用**交叉熵进行计算loss**，对于评估阶段，我们**计算熵，可以节省显存**

![image-20260222215836102](assets/image-20260222215836102.png)

![image-20260222221117303](assets/image-20260222221117303.png)

在 SFT 中我们最小化的损失是给定提示时目标输出的负对数似然。为了计算这个损  失,我们需要计算给定提示时目标输出的对数概率,并对输出中的所有标记进行求和,同时屏蔽提示中的  标记并填充标记。

##### 精度转换规则

计算Entropy时，转 FP32 防溢出，代码如下：在transformer中的RMSNORM中也有

对于计算乘方 ($x^2$, $x^3$) 与 指数 ($e^x$)，. 对数 ($\log(x)$) 与 除法 ($1/x$)，都需要转精度：

大规模累加 ($\sum$) 与 归约平均 (`mean`) —— 警惕“大数吃小数”

损失函数计算要全局强制 FP32

```
logits_fp32 = logits.to(torch.float32)
```

##### loss的返回

```
metadata = {
"loss": nll_sum.detach()
}
return scaled_loss.detach(), metadata
```

对于需要返回和记录的loss，都需要将他的计算图剥离出来，不剥离放入表中会让显存暴增，剥离以后会变成共享张量内存的死数据，不会有计算图。

像 `nll_sum`（负对数似然和）或者 `metadata` 里的数据，通常是作为监控指标（Metrics）返回给上层的。评估指标通常需要转换格式为numpy或wandb记录，PyTorch 规定，**带有计算图的张量是不能直接转换成 NumPy 数组的**，先执行 `.detach()` 是标准的安全操作。

#### 验证阶段的小tips

在生成文本时，要进行右对齐，即再左边标签打上padding.（让最后一个词能够精准预测）

在计算交叉熵是，要在右边标签打padding（位置编码的正确性，一句话开头应该是0）

```
torch.nn.utils.rnn.pad_sequence默认右对齐，左对齐用torch.nn.functional.pad，或者将句子倒转，然后打标签再反转
```

#### 打padding的技巧

SFT必须先补全再踢人，不然会踢掉一些原始数据。

response_masks是专门对label的掩码，因此要从1开始

对于计算Loss ，除了多轮batch的平均，还要除一个batch数量的平均

对于inputs和lable的补全，都需要填padding_id,不能自己填-100啥的，至于如何不加入交叉熵，补全句子的时候他就给了默认值是0了，因此padding位置就是0

最后要打上EOS Token，**把“停止输出”也作为一种可以被学习的知识，通过反向传播刻进模型的神经网络中。****下一个 Token 预测 EOS 的概率必须接近 100%”**。这样模型就不会把宝贵的神经元用来“脑补”答案之后的废话，而是更专注地学习前置的逻辑推理。

### 测试结果

#### 官方函数测试

![image-20260227164929300](assets/image-20260227164929300.png)

#### SFT在GSM8K的检验结果

我们使用GSM8K的train数据集进行训练，并使用他的测试集进行测试，测试了不同训练样本数量下的正确率，格式错误率，平均token长度。

| **数据集** | **训练样本数 (条件)** | **正确率** | **格式错误率** | **长度 (平均token)** | **成功样本平均长度**token | **错误回答平均长度**token |
| :--------: | :-------------------: | :--------: | :------------: | :------------------: | :-----------------------: | :-----------------------: |
|   GSM8K    |          128          |   69.07%   |     2.35%      |        151.23        |          104.79           |          254.91           |
|   GSM8K    |          256          |   70.36%   |     0.76%      |        142.41        |          110.42           |          218.32           |
|   GSM8K    |          512          |   71.72%   |     1.06%      |        161.04        |          115.26           |          277.17           |
|   GSM8K    |         1024          |   71.11%   |     0.76%      |        154.02        |          116.01           |          247.60           |

结论：

- **数据集难度与样本需求（核心结论）：** 这个数据集相对简单，模型仅需 **128个样本微调就能预测得很好**（正确率已达69.07%）。随着数据量增加到256、512甚至1024，正确率基本在70%~71%之间浮动，**后面并没有很大的性能提升**。这说明对于该任务，少量高质量数据即可激发模型的推理能力，存在明显的边际效用递减。
- **格式遵循能力极强：** 在128个样本下，模型的格式错误率就已降至2.35%。在256个样本及以上时，格式错误率稳定在1%左右甚至更低，说明模型能够极快地掌握所需的输出格式。
- **回答长度与模型状态的关联：** 各组实验均显示，**错误回答的平均长度（218~277 tokens）远大于正确回答的平均长度（104~117 tokens）**。这表明当模型无法迅速找到正确解题路径时，往往会进行更冗长的无效推理或陷入“过度思考”（overthinking）。

同样的，我们使用更高难度的Bespoke-Stratos-17k数据集进行训练与测试，注意这个数据集的验证集数量过少，因此是使用训练集截取的部分数据作为测试集

| **数据集** | **训练样本数 (条件)** | **正确率** | **格式错误率** | **长度 (平均)** | **成功样本平均长度** | **错误回答平均长度** |
| :--------: | :-------------------: | :--------: | :------------: | :-------------: | :------------------: | :------------------: |
| Bespoke17k |      128 (false)      |   17.50%   |     67.50%     |  465.77 tokens  |    236.69 tokens     |    514.37 tokens     |
| Bespoke17k |      256 (false)      |   34.50%   |     56.50%     | 1318.83 tokens  |    389.71 tokens     |    1808.21 tokens    |
| Bespoke17k |      512 (false)      |   29.00%   |     65.50%     | 2633.59 tokens  |    772.12 tokens     |    3393.92 tokens    |
| Bespoke17k |      512 (true)       |   31.50%   |     62.00%     | 2344.26 tokens  |    742.38 tokens     |    3080.90 tokens    |
| Bespoke17k |     1024 (false)      |   50.50%   |     41.00%     | 2195.61 tokens  |    1062.47 tokens    |    3351.64 tokens    |
| Bespoke17k |      1024 (true)      |   51.50%   |     42.00%     | 2198.28 tokens  |    1059.19 tokens    |    3407.82 tokens    |

结论：

- **数据集难度高，性能随数据量显著提升（核心结论）：** 与之前 GSM8K 仅需128条就能达到高正确率不同，Bespoke17k 显然是一个**难度大得多**的数据集。在128条样本时，正确率仅有 17.50%。但随着样本量增加到 1024，正确率大幅跃升至 51.50%。这说明在该数据集上，**模型尚未遇到性能瓶颈，增加训练样本数据量能带来非常显著的性能提升**。
- **格式学习极其困难：** 相比 GSM8K 极低的格式错误率，模型在 Bespoke17k 上表现出了严重的格式遵循问题。在128个样本时，格式错误率高达 67.50%；即便增加到了1024个样本，格式错误率依然维持在 41%~42% 的高位。这表明该数据集要求的输出格式可能非常复杂、冗长或反直觉，消耗了模型极大的学习成本，并且是导致整体得分偏低的主要原因之一。
- **长度极端化与严重的“过度思考” (Overthinking)：** Bespoke17k 的生成长度远超 GSM8K。随着模型能力的提升（样本数增加），成功解题的平均长度从约236 tokens 增长到了约 1060 tokens，说明正确推理需要较长的思维链。然而，**错误回答的长度极其夸张，在512和1024样本下飙升至 3000~3400 tokens 以上**。这强烈暗示模型在面对无法解答的复杂问题时，不仅没有及时停止，反而陷入了严重的无限循环、幻觉堆砌或无意义的冗长推理中。
- 可以看到提炼正确样本进行训练是有意义的。





最后，为了测试模型的泛化能力，我们使用Bespoke-Stratos-17k数据集训练，测试他们在AIME、MATH (math-500)、AMC、OmniMATH的能力，我们默认，跑两个epoch，每轮2048条样本，每个数据集的评测数据集全部上，表格如下：

| **测试数据集 (条件)** | **正确率** | **格式错误率** | **长度 (平均)** | **成功样本平均长度** | **错误回答平均长度** |
| :-------------------: | :--------: | :------------: | :-------------: | :------------------: | :------------------: |
|   AIME 2024 (false)   |   3.33%    |     76.67%     | 3060.27 tokens  |    1277.00 tokens    |    3121.76 tokens    |
|   AIME 2025 (false)   |   3.33%    |     73.33%     | 3192.47 tokens  |    973.00 tokens     |    3269.00 tokens    |
|      AMC (false)      |   19.28%   |     65.06%     | 2957.77 tokens  |    1148.69 tokens    |    3389.79 tokens    |
|     GSM8K (false)     |   55.60%   |     38.00%     | 2081.32 tokens  |    971.46 tokens     |    3471.15 tokens    |
|   MATH-500 (false)    |   42.00%   |     47.40%     | 2393.41 tokens  |    1126.70 tokens    |    3310.68 tokens    |
|   OmniMATH (false)    |   10.20%   |     63.60%     | 2828.42 tokens  |    1251.69 tokens    |    3007.52 tokens    |
|   AIME 2024 (true)    |   6.67%    |     73.33%     | 3093.33 tokens  |    1467.50 tokens    |    3209.46 tokens    |
|   AIME 2025 (true)    |   6.67%    |     70.00%     | 3207.53 tokens  |    1582.50 tokens    |    3323.61 tokens    |
|      AMC (true)       |   9.64%    |     75.90%     | 3087.57 tokens  |    1278.00 tokens    |    3280.59 tokens    |
|     GSM8K (true)      |   52.60%   |     40.20%     | 2160.37 tokens  |    982.01 tokens     |    3468.00 tokens    |
|    MATH-500 (true)    |   38.00%   |     53.60%     | 2573.75 tokens  |    1180.45 tokens    |    3427.71 tokens    |
|    OmniMATH (true)    |   9.00%    |     72.40%     | 3012.72 tokens  |    1126.56 tokens    |    3199.26 tokens    |

**泛化能力与数据集难度呈强负相关：** 模型在 Bespoke17k 上训练后，在跨数据集评估中表现出了明显的梯队差异。在相对基础的 GSM8K 数据集上，模型达到了最高约 55.60% 的正确率 。在 MATH-500 上正确率下降至约 38% - 42% 。而在难度极高的竞赛级数据集（如 AIME 2024、AIME 2025 和 OmniMATH）上，模型的正确率暴跌，最低仅为 3.33% 。这说明模型虽然学到了部分数学推理能力，但尚不足以应对极其复杂的竞赛级难题。

**摘除错误样本（True）并非总是正向收益，甚至在多数场景下起反作用：**

- **在常规及中等难度基准上（性能下降）：** 在 GSM8K、MATH-500、AMC 和 OmniMATH 上，保留所有样本（`false`）的表现均**优于**摘除错误样本（`true`）。以 AMC 为例，保留错误样本的正确率为 19.28%，而摘除后反而暴跌至 9.64%；GSM8K 和 MATH-500 也有不同程度的下降。这表明：**带有错误的探索轨迹可能包含了有价值的试错信号（Negative / Contrastive signals），或者单纯是因为保留错误样本维持了更大的数据多样性和数据量**（**这个不太可能，因为添加的不多**），从而帮助模型获得了更鲁棒的推理能力。
- **在极高难度基准上（微弱提升）：** 仅在 AIME 2024 和 AIME 2025 这种竞赛级天花板难度上，摘除错误样本（`true`）让正确率从 3.33% 提升到了 6.67%（由于测试集样本少，这通常对应多答对了一两道题）。这暗示在面对极度复杂的逻辑时，错误轨迹可能确实会引发模型的“逻辑混乱”或“幻觉模仿”，此时保证训练语料的绝对正确性才显现出一点优势。



#### MATH专家迭代

滤掉 SFT 数据集中的错误示例可以提升模型性能 。专家迭代更进一步：**我们将这种过滤机制直接应用到基础模型自己生成的推理轨迹上** 。这种方法在文献中被称为“专家迭代” 。

**输入**：初始策略模型 $\pi_{\theta_{init}}$，奖励函数 $R$，以及任务问题集 $D$ 。

**循环过程**（共进行 `n_ei_steps` 次迭代）：

1. 从问题集 $D$ 中采样一个批次的问题 $\mathcal{D}_{b}$ 。
2. 固定当前模型为“旧策略” $\pi_{\theta_{old}}$ 。
3. **采样（Rollout）**：对于批次中的每一个问题 $q$，让旧策略模型生成 $G$ 个不同的回答 $\{o^{(i)}\}_{i=1}^{G}$ 。
4. **评分与过滤**：使用奖励函数 $R(q,o^{(i)})$ 对每个回答进行评分，得到奖励 $\{r^{(i)}\}_{i=1}^{G}$ 。将得分为 $0$（错误）的回答丢弃，只保留完全正确的问答对，构建出一个新的、由模型自己生成的正确数据集 $\mathcal{D}_{sft}$ 。
5. **更新模型**：使用这个新构建的高质量数据集 $\mathcal{D}_{sft}$，通过 SFT（监督微调）算法来更新当前模型 $\pi_{\theta}$ 
